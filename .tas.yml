---
framework: jest
preRun:
     command:
        - npm ci
        - npm run build
preMerge:
  pattern:
    - "./test/utils/warning.spec.ts"
postMerge:
  pattern:
    - "./test/utils/warning.spec.ts"
postRun:
     command:
        - node --version
version: 1.0
